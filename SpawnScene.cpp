#include "SpawnScene.h"

bool SpawnScene::Update()
{
	return false;
}

SpawnScene::SpawnScene(Camera* camera, Player* player, Enemy* enemy, int& BackModel, int& MapModel, fps* Fps, Box* Fade, Shadow* shadow)
{
}
